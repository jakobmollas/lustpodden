# Lustpodden Search
Podcast Search — Eleventy + GitHub Actions Template

Trigger the workflow manually the first time: Actions > Build and Deploy Podcast Site > Run workflow (or push to `main`).


## Local development

```bash
npm install
npm run build
npm run start
# open http://localhost:8080